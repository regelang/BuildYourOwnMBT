/// ================================================================================
/// 恭喜你，至此我们已经完成了大部分Token的词法分析了，现在我们来完成字符串的词法分析。
///
/// 在MiniMoonBit中，字符串是由""包裹的任意文本，例如"hello"，"123", "..."。
///
/// 当我们遇到第一个"时，开启一个循环，一直读取后面的所有字符，直到再次遇到"为止。
///
/// 在真正的工业级编译中，需要注意处理转义字符的情况，例如"hello\"world"中的\"不应该被当做字符串的结尾。
/// 此外还需要处理换行符等情况。
///
/// 不过为了简单起见，我们这里的MiniMoonbit不处理转义字符，当你读取到"时，就可以认为字符串结束了。
///
/// 现在继续修改`lexer/lexer.mbt`文件，完成`tokenize`函数吧。
///
/// 在你实现完成后，可以使用moon test命令来运行所有测试：
///
/// 或者用下面的命令来单独运行当前测试：
///
/// ```
/// moon test -p lexer -f tokenize_string_test.mbt
/// ```
///
/// 祝你成功！🚀
/// ================================================================================

///|
test "Tokenize String" {
  let code =
    #|"Hello"
    #|"This is a test string"
    #|"Hello world!"
    #|"MiniMoonbit2025"
  let tokens = tokenize(code)
  assert_true(tokens.length() is 5)
  assert_true(tokens[0].kind is String("Hello"))
  assert_true(tokens[1].kind is String("This is a test string"))
  assert_true(tokens[2].kind is String("Hello world!"))
  assert_true(tokens[3].kind is String("MiniMoonbit2025"))
  assert_true(tokens[4].kind is EOF)
}
