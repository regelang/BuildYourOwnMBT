/// ================================================================================
/// 恭喜你完成了整数的词法分析器，现在我们来处理浮点数。
///
/// 浮点数是编程语言中表示小数的常用方式。它们通常由一个整数部分、小数点和一个小数部分组成。
///
/// 例如，3.14、0.001和2.5都是有效的浮点数。
///
/// 在不少语言中，浮点数尝尝会有比较复杂的表示方式，比如科学计数法（如1.5e10）。
/// 但在我们的MiniMoonBit中，我们只需要处理简单的小数表示即可。
///
/// 如何实现浮点数的词法分析呢？
///
/// 当我们遇到一个数字时，我们循环读取所有连续的数字字符。
/// 如果没有遇到小数点（`.`），我们就把它当作整数处理。
///
/// 当遇到小数点（`.`）时，我们继续读取后续的数字字符，直到遇到非数字字符为止。
/// 然后，我们将整数部分和小数部分组合起来，形成一个完整的浮点数。
///
/// 那么如何把一个字符串转换为浮点数呢？
///
/// MoonBit的标准库中，已经为你提供了将String转换成浮点数的工具: @strconv.parse_double
///
/// 例如：
///
/// ```moonbit no test
/// let s : String = "3.14"
/// let f : Double = @strconv.parse_double(s)
/// assert_eq(f, 3.14)
/// ```
///
/// 请查阅标准库文档https://mooncakes.io/docs/moonbitlang/core/strconv
///
/// 现在继续修改`lexer/lexer.mbt`文件，完成`tokenize`函数吧。
///
/// 在你实现完成后，可以使用moon test命令来运行所有测试：
///
/// 或者用下面的命令来单独运行当前测试：
/// 
/// ```
/// moon test -p lexer -f tokenize_float_test.mbt
/// ```
///
/// 祝你成功！🚀
/// ================================================================================

///|
test "Tokenize Float Number" {
  let code =
    #|3.14 0.0 2.718 1.6180339887 0.57721 42.0
  let tokens = tokenize(code)
  assert_true(tokens.length() is 7)
  assert_true(tokens[0].kind is Double(3.14))
  assert_true(tokens[1].kind is Double(0.0))
  assert_true(tokens[2].kind is Double(2.718))
  assert_true(tokens[3].kind is Double(1.6180339887))
  assert_true(tokens[4].kind is Double(0.57721))
  assert_true(tokens[5].kind is Double(42.0))
  assert_true(tokens[6].kind is EOF)
}
